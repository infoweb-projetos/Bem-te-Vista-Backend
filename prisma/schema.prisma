generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


model User {
  id String @id @default(uuid())
  nome String
  nome_de_usuario String @unique
  email String @unique
  senha String

  @@map("Usuarios")
}